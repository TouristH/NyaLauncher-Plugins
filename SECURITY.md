# 安全策略

不要在公开 Issue 中披露尚未修复、可被主动利用的插件供应链漏洞。请通过
[NyaLauncher 主仓库的安全报告渠道](https://github.com/redstore-noob/NyaLauncher/security/advisories/new)
联系维护者，并附上插件 ID、受影响版本和证据。

确认存在风险后，中心维护者会将版本标记为 `yanked` 并说明原因。历史版本文件
不会静默删除或改写；如果该版本已有管理员审核，可信审核者还会删除对应的
`reviews/<插件 ID>/<版本>.json`，使生成索引不再带有 `release.review`。修复后的插件
必须发布新版本和新哈希，并重新进行版本级审核。

管理员审核仅绑定到精确的插件 ID、版本与 SHA-256，表示审核者核对过该固定资产及其
声明，不构成漏洞不存在、代码绝对安全或持续安全的保证。
